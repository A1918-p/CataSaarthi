[project]
name = "catasaarthi"
version = "0.1.0"
description = "CataSaarthi AI - fundus cataract screening pipeline"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]